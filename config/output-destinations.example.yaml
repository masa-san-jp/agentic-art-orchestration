contract_version: output-destinations/v1
profile: local
destinations:
  state_root: /absolute/path/to/orchestration-state
  internal_output_root: /absolute/path/to/internal-output
  # When set, canonical PLAN_READY/PASSED plan records are projected locally.
  # Git commit, push, release, and repository visibility remain human-gated.
  public_projection_root: /absolute/path/to/public-project
